{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Backend for client component",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db.start": "json-server-auth db.json --routes custom-routes.json --port 8080"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0",
    "express": "^4.18.2"
  },
  "author": "EDV",
  "license": "ISC"
}
